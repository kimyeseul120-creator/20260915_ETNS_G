# Supabase Postgres 연결 문자열 (Project Settings > Database > Connection string > URI)
# 실제 값은 이 파일이 아니라 .env 파일(gitignore 처리됨)에만 넣으세요.
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:6543/postgres
